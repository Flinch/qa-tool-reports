# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: generated/p9-e2e-critical-flows/p9-tc-33-client-views-automation-runs-and-downloads-a-report-for-a-co.spec.ts >> TC-33 — Client views automation runs and downloads a report for a completed run >> TC-33: Client views automation runs and downloads a report for a completed run
- Location: tests/generated/p9-e2e-critical-flows/p9-tc-33-client-views-automation-runs-and-downloads-a-report-for-a-co.spec.ts:26:3

# Error details

```
TimeoutError: locator.check: Timeout 30000ms exceeded.
Call log:
  - waiting for getByRole('checkbox', { name: /TC-80/ })

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - complementary [ref=e4]:
    - generic [ref=e5]: Blueprint
    - generic [ref=e6]:
      - generic [ref=e7]: Navigation
      - link "Dashboard" [ref=e8] [cursor=pointer]:
        - /url: /
        - img [ref=e9]
        - text: Dashboard
      - link "Projects" [ref=e11] [cursor=pointer]:
        - /url: /projects
        - img [ref=e12]
        - text: Projects
    - generic [ref=e14]:
      - generic [ref=e15]: Project
      - link "Views" [ref=e16] [cursor=pointer]:
        - /url: /projects/4/views
        - img [ref=e17]
        - text: Views
    - generic [ref=e21]:
      - button "Change profile picture" [ref=e22] [cursor=pointer]:
        - generic [ref=e23]: T
      - generic [ref=e24]:
        - generic "Test Engineer — click to edit" [ref=e25] [cursor=pointer]: Test
        - generic "QA Engineer — click to edit" [ref=e26] [cursor=pointer]: QA Engineer
      - button "Log out" [ref=e27] [cursor=pointer]:
        - img [ref=e28]
  - main [ref=e31]:
    - button "Search test cases and bugs" [ref=e32] [cursor=pointer]:
      - img [ref=e33]
      - generic [ref=e36]: Search
      - generic [ref=e37]: /
    - generic [ref=e38]:
      - generic [ref=e39]:
        - button "Back" [ref=e40] [cursor=pointer]:
          - img [ref=e41]
        - generic [ref=e43]:
          - link "Projects" [ref=e44] [cursor=pointer]:
            - /url: /projects
          - generic [ref=e45]: /
          - link "Sauce Labs Demo App" [ref=e46] [cursor=pointer]:
            - /url: /projects/4
          - generic [ref=e47]: /
          - generic [ref=e48]: Executions
      - generic [ref=e49]:
        - link "See bugs" [ref=e50] [cursor=pointer]:
          - /url: /projects/4/bugs
          - text: See bugs
          - img [ref=e51]
        - button "Archived" [ref=e53] [cursor=pointer]
        - button "+ New execution run" [ref=e54] [cursor=pointer]
    - generic [ref=e55]:
      - generic [ref=e57]:
        - generic [ref=e58]: Platform
        - button "All" [ref=e59] [cursor=pointer]
        - button "Web" [ref=e60] [cursor=pointer]
        - button "iOS" [ref=e61] [cursor=pointer]
        - button "Android" [ref=e62] [cursor=pointer]
      - generic [ref=e64]:
        - link "Release regression" [ref=e65] [cursor=pointer]:
          - /url: /projects/4/executions/1
        - generic [ref=e66]:
          - generic [ref=e67]: Release regression
          - generic [ref=e68]:
            - generic [ref=e69]: Web
            - generic [ref=e70]: Completed
            - button "Run actions" [ref=e72] [cursor=pointer]:
              - img [ref=e73]
        - generic [ref=e78]:
          - generic [ref=e79]: 4 test cases · 0 suites
          - generic [ref=e80]:
            - img [ref=e81]
            - text: v1.4.0
        - generic [ref=e87]:
          - generic [ref=e88]: 3 passed
          - text: ","
          - generic [ref=e89]: 1 failed
        - generic [ref=e90]:
          - generic [ref=e91]: Created 8/18/2026 · Completed 8 hours ago
          - button "Re-execute" [ref=e92] [cursor=pointer]
    - generic [ref=e94]:
      - generic [ref=e95]: New execution run
      - generic [ref=e96]:
        - generic [ref=e97]: Name
        - textbox "e.g. Sprint 14 regression" [active] [ref=e98]: TC-33 in-progress run 1787111926719
      - generic [ref=e99]:
        - generic [ref=e100]: Platform *
        - combobox [ref=e101]:
          - option "Select a platform..."
          - option "Web"
          - option "iOS" [selected]
          - option "Android"
        - generic [ref=e102]: An execution run represents one platform's release state — test cases and suites below are scoped to it.
      - generic [ref=e103]:
        - generic [ref=e104]: Version
        - textbox "e.g. 2.4.1 (optional)" [ref=e105]
        - generic [ref=e106]: Which build/release this run is testing — used to scope the Quality Score once a tracked version is set for this platform.
      - generic [ref=e107]:
        - generic [ref=e108] [cursor=pointer]:
          - checkbox "Exclude from Quality Score" [ref=e109]
          - text: Exclude from Quality Score
        - generic [ref=e110]: For a run that isn't real coverage signal — a broken environment, a one-off smoke pass. Results still record normally; they just won't affect the score. Can be changed later from the run's own page.
      - generic [ref=e111]:
        - generic [ref=e113]: Test cases
        - generic [ref=e114]: No test cases in this project yet.
      - generic [ref=e115]:
        - generic [ref=e116]: By feature
        - generic [ref=e117]:
          - generic [ref=e118] [cursor=pointer]:
            - checkbox "Authentication 4 tests" [ref=e119]
            - generic [ref=e120]: Authentication
            - generic [ref=e121]: 4 tests
          - generic [ref=e122] [cursor=pointer]:
            - checkbox "Automation 0 tests" [ref=e123]
            - generic [ref=e124]: Automation
            - generic [ref=e125]: 0 tests
          - generic [ref=e126] [cursor=pointer]:
            - checkbox "Bug tracking 0 tests" [ref=e127]
            - generic [ref=e128]: Bug tracking
            - generic [ref=e129]: 0 tests
      - generic [ref=e130]:
        - generic [ref=e131]: By test suite
        - generic [ref=e132]: No test suites with ios test cases in this project yet.
      - generic [ref=e133]:
        - generic [ref=e134]: Automation suites
        - generic [ref=e135]: No ios automation suites in this project yet.
      - generic [ref=e136]:
        - button "Cancel" [ref=e137] [cursor=pointer]
        - button "Create run" [disabled] [ref=e138]
  - link "Engineering" [ref=e140] [cursor=pointer]:
    - /url: /projects/4/engineering
    - img [ref=e141]
```

# Test source

```ts
  67  |       const runRows = client.locator('.run-row');
  68  |       await expect(runRows.first()).toBeVisible();
  69  |       expect(await runRows.count()).toBeGreaterThan(0);
  70  |       await expect(client.getByText(/Run failed|passed/).first()).toBeVisible();
  71  |     });
  72  | 
  73  |     // 4. Verify no controls exist to trigger a run, re-run, cancel, delete a
  74  |     // run, or change nightly scheduling
  75  |     await test.step('Verify no controls exist to trigger a run, re-run, cancel, delete a run, or change nightly scheduling', async () => {
  76  |       await expect(client.getByRole('button', { name: 'Run suite' })).toHaveCount(0);
  77  |       await expect(client.getByRole('button', { name: '+ New suite' })).toHaveCount(0);
  78  |       await expect(client.getByRole('button', { name: 'Maintenance mode' })).toHaveCount(0);
  79  |       await expect(client.getByRole('button', { name: /Re-?run/i })).toHaveCount(0);
  80  |       await expect(client.getByRole('button', { name: /Cancel/i })).toHaveCount(0);
  81  |       await expect(client.getByRole('button', { name: /Delete/i })).toHaveCount(0);
  82  |       await expect(client.getByRole('button', { name: 'Nightly' })).toHaveCount(0);
  83  | 
  84  |       // The per-run "More actions" menu exposes only a view link for a
  85  |       // client — no re-run/cancel/delete/schedule action.
  86  |       // (Previously expected 'CI logs'; the link is now labelled 'View report'.)
  87  |       await client.locator('.run-row').first().getByRole('button', { name: 'More actions' }).click();
  88  |       await expect(client.getByRole('link', { name: 'View report' }).first()).toBeVisible();
  89  |       await expect(client.getByRole('button', { name: /Re-?run|Cancel|Delete|Schedule/i })).toHaveCount(0);
  90  |       await client.keyboard.press('Escape');
  91  |     });
  92  | 
  93  |     // 5. Open a completed execution run on the executions page
  94  |     //
  95  |     // Corrected 2026-08-17: the plan's report-download control (steps 5-7) is
  96  |     // on the EXECUTIONS page (manually-executed test cases), not the
  97  |     // Automation page checked in steps 2-4 above. Those are two different
  98  |     // surfaces in this app: the Automation page's completed-run panel has a
  99  |     // "Full report" link (target="_blank", no download attribute — opens an
  100 |     // externally-hosted Maestro HTML report to VIEW, confirmed live it never
  101 |     // fires a Playwright 'download' event), while the Executions page's
  102 |     // completed-run detail has its own "Download report" BUTTON that
  103 |     // genuinely triggers a file download (confirmed live: clicking it
  104 |     // downloaded a real .pdf). A prior heal conflated the two and wrongly
  105 |     // flagged this as a regression.
  106 |     await test.step('Open a completed execution run on the executions page', async () => {
  107 |       await client.goto(`/projects/${projectId}/executions`);
  108 |       await dismissTourIfPresent(client);
  109 |       // FRAGILE: execution run rows carry no ARIA role/accessible name in
  110 |       // this app (plain divs wrapping a link) — .card is the one
  111 |       // hand-authored class confirmed live to scope an executions-page run
  112 |       // row (a different entity/class than the Automation page's .run-row).
  113 |       // Any card whose "Completed"/"Not started" status badge reads
  114 |       // "Completed" is a genuinely completed run with results.
  115 |       const completedRun = client.locator('.card').filter({ hasText: 'Completed' }).first();
  116 |       // Navigate via the card's own link href rather than click()/
  117 |       // dispatchEvent('click'): this card is a "stretched link" (an <a>
  118 |       // absolutely positioned to cover the whole card). Confirmed live
  119 |       // Playwright's real mouse-simulated click() (move, then down/up)
  120 |       // lands inconsistently under real-world timing/CPU contention
  121 |       // (reproduced running the full suite in parallel — no click error,
  122 |       // but no navigation), and dispatchEvent('click') never navigates at
  123 |       // all (a synthetic, non-trusted click event doesn't trigger an
  124 |       // anchor's default navigation). Reading the real href and navigating
  125 |       // to it is the reliable equivalent of "open this run".
  126 |       const href = await completedRun.locator('a').first().getAttribute('href');
  127 |       expect(href).toBeTruthy();
  128 |       await client.goto(href!);
  129 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/executions/\\d+$`));
  130 |     });
  131 | 
  132 |     // 6. Verify a report download control is present on the completed run
  133 |     await test.step('Verify a report download control is present on the completed run', async () => {
  134 |       await expect(client.getByRole('button', { name: /Download report/i })).toBeVisible();
  135 |     });
  136 | 
  137 |     // 7. Click the report download control and confirm a report download is
  138 |     // initiated
  139 |     await test.step('Click the report download control and confirm a report download is initiated', async () => {
  140 |       const [download] = await Promise.all([
  141 |         client.waitForEvent('download'),
  142 |         client.getByRole('button', { name: /Download report/i }).click(),
  143 |       ]);
  144 |       expect(download.suggestedFilename()).toBeTruthy();
  145 |     });
  146 | 
  147 |     // 8. As the test engineer identity (default authenticated page), create an
  148 |     // execution run for the SauceLabs Demo project
  149 |     //
  150 |     // Not helpers/project-9/createExecutionRun.ts: that helper dispatches an
  151 |     // AUTOMATION-page suite run (a different entity from an executions-page
  152 |     // "execution run", confirmed live — see step 5's comment), so it cannot
  153 |     // produce the in-progress run steps 10-11 need on the EXECUTIONS page.
  154 |     // Inlined here instead of a new helper per the healing-rules boundary
  155 |     // (no new helpers during a heal) — worth the reviewer's attention if a
  156 |     // future spec needs this same setup again.
  157 |     let runName = '';
  158 |     await test.step('As the test engineer identity, create an execution run for the SauceLabs Demo project', async () => {
  159 |       runName = `TC-33 in-progress run ${Date.now()}`;
  160 |       await page.goto(`/projects/${projectId}/executions`);
  161 |       await dismissTourIfPresent(page);
  162 |       await page.getByRole('button', { name: '+ New execution run' }).click();
  163 |       await page.getByPlaceholder('e.g. Sprint 14 regression').fill(runName);
  164 |       await page.getByRole('combobox').selectOption({ label: 'iOS' });
  165 |       // One manual test case is enough to have something "not run" once the
  166 |       // run is left in progress.
> 167 |       await page.getByRole('checkbox', { name: /TC-80/ }).check();
      |                                                           ^ TimeoutError: locator.check: Timeout 30000ms exceeded.
  168 |       await page.getByRole('button', { name: 'Create run' }).click();
  169 |       await expect(page.getByRole('link', { name: runName })).toBeVisible();
  170 |     });
  171 | 
  172 |     // 9. Leave the newly created run in progress; do not wait for it to
  173 |     // complete
  174 |     await test.step('Leave the newly created run in progress; do not wait for it to complete', async () => {
  175 |       // No-op: the run was just created with its one test case left "Not
  176 |       // run"/"Not started" — nothing here executes it.
  177 |     });
  178 | 
  179 |     // 10. As the client from step 1, navigate to that in-progress run on the
  180 |     // executions page
  181 |     await test.step('As the client from step 1, navigate to that in-progress run on the executions page', async () => {
  182 |       await client.goto(`/projects/${projectId}/executions`);
  183 |       await dismissTourIfPresent(client);
  184 |       const newRun = client.getByRole('link', { name: runName });
  185 |       await expect(newRun).toBeVisible();
  186 |       // Navigate via href — same stretched-link click reliability
  187 |       // consideration as step 5.
  188 |       const href = await newRun.getAttribute('href');
  189 |       expect(href).toBeTruthy();
  190 |       await client.goto(href!);
  191 |       await expect(client.getByRole('heading', { name: runName })).toBeVisible();
  192 |     });
  193 | 
  194 |     // 11. Verify no report download control is present on the in-progress run
  195 |     await test.step('Verify no report download control is present on the in-progress run', async () => {
  196 |       await expect(client.getByRole('button', { name: /Download report/i })).toHaveCount(0);
  197 |     });
  198 |   });
  199 | });
  200 | 
```