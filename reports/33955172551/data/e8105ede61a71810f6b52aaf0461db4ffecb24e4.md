# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: generated/p14-critical-flows/p14-tc-3-company-record-detail-view-shows-linked-people-and-opportuni.spec.ts >> TC-3 — Company record detail view shows linked People and Opportunities sections >> TC-3: Company record detail view shows linked People and Opportunities sections
- Location: tests/generated/p14-critical-flows/p14-tc-3-company-record-detail-view-shows-linked-people-and-opportuni.spec.ts:7:3

# Error details

```
Error: expect(page).toHaveTitle(expected) failed

Expected pattern: /Companies/
Received string:  "Twenty"
Timeout: 5000ms

Call log:
  - Expect "toHaveTitle" with timeout 5000ms
    5 × unexpected value "Twenty"

```

```yaml
- button "B Blueprint Audit":
  - text: B Blueprint Audit
  - img
- button "Search"
- button "Collapse navigation panel"
- tablist "Navigation tabs":
  - tab "Home" [selected]:
    - img
  - tab "Chat":
    - img
- button "New chat":
  - img
  - text: New chat
- text: ‌ ‌ ‌ ‌ ‌ ‌ ‌ ‌ ‌
- complementary
```

# Test source

```ts
  1  | // spec: specs/p14-tc-3-company-record-detail-view-shows-linked-people-and-opportuni.md
  2  | // seed: tests/seed.spec.ts
  3  | 
  4  | import { test, expect } from '@playwright/test';
  5  | 
  6  | test.describe('TC-3 — Company record detail view shows linked People and Opportunities sections', () => {
  7  |   test('TC-3: Company record detail view shows linked People and Opportunities sections', async ({ page }) => {
  8  |     // 1. As default test user, navigate to /objects/companies.
  9  |     await test.step('As default test user, navigate to /objects/companies.', async () => {
  10 |       await page.goto('/objects/companies');
> 11 |       await expect(page).toHaveTitle(/Companies/);
     |                          ^ Error: expect(page).toHaveTitle(expected) failed
  12 |     });
  13 | 
  14 |     // 2. Locate the row with the company name 'Stripe'.
  15 |     await test.step("Locate the row with the company name 'Stripe'.", async () => {
  16 |       await expect(page.getByRole('link', { name: 'Stripe', exact: true })).toBeVisible();
  17 |     });
  18 | 
  19 |     // 3. Click the 'Expand record' control on the 'Stripe' row.
  20 |     await test.step("Click the 'Expand record' control on the 'Stripe' row.", async () => {
  21 |       // Click the Stripe company name to open the right-side detail panel,
  22 |       // then click the 'Expand record' button inside the panel to open the full-page view.
  23 |       await page.getByRole('link', { name: 'Stripe', exact: true }).click();
  24 |       await page.getByRole('button', { name: 'Expand record' }).click();
  25 |     });
  26 | 
  27 |     // 4. Assert the page URL changes to the Stripe company detail route.
  28 |     await test.step('Assert the page URL changes to the Stripe company detail route.', async () => {
  29 |       await expect(page).toHaveURL(/\/object\/company\//);
  30 |     });
  31 | 
  32 |     // 5. Assert the page contains a section labelled 'People'.
  33 |     await test.step("Assert the page contains a section labelled 'People'.", async () => {
  34 |       await expect(page.getByRole('button', { name: /People See all people/ })).toBeVisible();
  35 |     });
  36 | 
  37 |     // 6. Assert the People section contains an entry with the text 'Patrick Collison'.
  38 |     await test.step("Assert the People section contains an entry with the text 'Patrick Collison'.", async () => {
  39 |       await expect(page.getByRole('link', { name: 'P Patrick Collison' })).toBeVisible();
  40 |     });
  41 | 
  42 |     // 7. Assert the page contains a section labelled 'Opportunities'.
  43 |     await test.step("Assert the page contains a section labelled 'Opportunities'.", async () => {
  44 |       await expect(page.getByRole('button', { name: /Opportunities See all/ })).toBeVisible();
  45 |     });
  46 | 
  47 |     // 8. Assert the Opportunities section contains an entry with the text 'Platform Migration'.
  48 |     await test.step("Assert the Opportunities section contains an entry with the text 'Platform Migration'.", async () => {
  49 |       await expect(page.getByRole('link', { name: 'P Platform Migration' })).toBeVisible();
  50 |     });
  51 | 
  52 |     // 9. Assert the Opportunities section contains an entry with the text 'API Integration Deal'.
  53 |     await test.step("Assert the Opportunities section contains an entry with the text 'API Integration Deal'.", async () => {
  54 |       await expect(page.getByRole('link', { name: 'A API Integration Deal' })).toBeVisible();
  55 |     });
  56 |   });
  57 | });
  58 | 
```