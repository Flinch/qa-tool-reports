# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth-setups/project-9.setup.ts >> authenticate
- Location: tests/auth-setups/project-9.setup.ts:14:1

# Error details

```
Error: expect(page).not.toHaveURL(expected) failed

Expected pattern: not /\/login/
Received string: "https://qa-tool-client-staging.vercel.app/login"
Timeout: 5000ms

Call log:
  - Expect "not toHaveURL" with timeout 5000ms
    14 × unexpected value "https://qa-tool-client-staging.vercel.app/login"

```

```yaml
- link "Blueprint home":
  - /url: /
  - text: Blueprint
- text: Quality, made visible Welcome Log in to Blueprint Enter your workspace.
- button "Continue as guest"
- text: Or log in Email
- textbox "Email":
  - /placeholder: you@company.com
  - text: test_engineer@tester.com
- text: Password
- textbox "Password":
  - /placeholder: ••••••••
  - text: ME5z2S-YW1XNPAb-0prJwZAF
- text: Invalid email or password
- button "Log in"
```

# Test source

```ts
  1  | import { test as setup, expect } from '@playwright/test';
  2  | 
  3  | const authFile = process.env.STORAGE_STATE || '.auth/user.json';
  4  | 
  5  | // Blueprint (https://www.blueprintqa.tech/) login. "/" is a public marketing
  6  | // landing page (see client's LandingPage.jsx) with no login form on it — the
  7  | // form lives on its own "/login" route. Confirmed live (2026-08-14): every
  8  | // generated/heal/auth-setup run against project 9 started failing this exact
  9  | // step, 30s timeout waiting for the Email textbox, the moment "/" stopped
  10 | // being the login form.
  11 | const EMAIL = process.env.TEST_USER_NAME;
  12 | const PASSWORD = process.env.TEST_USER_PASSWORD;
  13 | 
  14 | setup('authenticate', async ({ page }) => {
  15 |   await page.goto('/login');
  16 | 
  17 |   await page.getByRole('textbox', { name: 'Email' }).fill(EMAIL ?? '');
  18 |   await page.getByRole('textbox', { name: 'Password' }).fill(PASSWORD ?? '');
  19 |   await page.getByRole('button', { name: 'Log in' }).click();
  20 | 
  21 |   // Generic post-login assertion: the page navigated away from the login URL.
  22 |   // The login form lives at /login; a successful login redirects to the
  23 |   // dashboard. Checking the URL is generic — true of any successful login on
  24 |   // any app — and does not depend on app-specific post-login UI text.
> 25 |   await expect(page).not.toHaveURL(/\/login/);
     |                          ^ Error: expect(page).not.toHaveURL(expected) failed
  26 | 
  27 |   await page.context().storageState({ path: authFile });
  28 | });
  29 | 
```