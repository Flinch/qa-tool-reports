# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: generated/e2e-critical-flows/tc-31-client-signs-in-and-lands-on-their-project-dashboard-showing.spec.ts >> TC-31 — Client signs in and lands on their project dashboard showing quality intelligence >> TC-31: Client signs in and lands on their project dashboard showing quality intelligence
- Location: tests/generated/e2e-critical-flows/tc-31-client-signs-in-and-lands-on-their-project-dashboard-showing.spec.ts:7:3

# Error details

```
Error: Could not authenticate test account "client_single_project": locator.fill: Timeout 30000ms exceeded.
Call log:
  - waiting for getByRole('textbox', { name: 'Email' })

```

# Test source

```ts
  57  |   credentialsFor(alias: string): AccountCredentials
  58  |   /** An authenticated context for this alias, created once per worker. */
  59  |   contextAs(alias: string): Promise<BrowserContext>
  60  |   /** A fresh page in this alias's authenticated context. */
  61  |   pageAs(alias: string): Promise<Page>
  62  | }
  63  | 
  64  | function envKey(alias: string, field: 'USERNAME' | 'PASSWORD'): string {
  65  |   return `TEST_ACCOUNT_${alias.toUpperCase()}_${field}`
  66  | }
  67  | 
  68  | export function credentialsFromEnv(alias: string): AccountCredentials {
  69  |   if (!/^[a-z][a-z0-9_]*$/.test(alias)) {
  70  |     throw new Error(
  71  |       `Invalid account alias "${alias}". Aliases are lowercase letters, digits and underscores ` +
  72  |       `(they become env var names), and must match an alias registered for this project.`
  73  |     )
  74  |   }
  75  | 
  76  |   const username = process.env[envKey(alias, 'USERNAME')]
  77  |   const password = process.env[envKey(alias, 'PASSWORD')]
  78  | 
  79  |   if (!username || !password) {
  80  |     // Names the exact fix rather than failing as a generic undefined. The
  81  |     // realistic causes are a spec referencing an alias nobody registered, or
  82  |     // a CI job that did not run fetch-run-config.js.
  83  |     throw new Error(
  84  |       `No credentials for account alias "${alias}". Expected ${envKey(alias, 'USERNAME')} and ` +
  85  |       `${envKey(alias, 'PASSWORD')} in the environment.\n` +
  86  |       `Add "${alias}" to this project's test accounts, or check that this job ran ` +
  87  |       `.github/scripts/fetch-run-config.js before the tests.`
  88  |     )
  89  |   }
  90  | 
  91  |   return { alias, username, password }
  92  | }
  93  | 
  94  | export type AccountsFixtureOptions = {
  95  |   /**
  96  |    * Extra context options for every account context. Contexts created via
  97  |    * browser.newContext() do NOT inherit a project's `use` block, so anything
  98  |    * layout-affecting (viewport, device scale) must be repeated here or the
  99  |    * second actor can silently get a different rendering — and therefore
  100 |    * different locators — than the first.
  101 |    */
  102 |   contextOptions?: BrowserContextOptions
  103 | }
  104 | 
  105 | // Mirrors playwright.config.js's own `use.baseURL` line. The accounts
  106 | // fixture is worker-scoped and Playwright forbids a worker fixture from
  107 | // depending on `baseURL`, which is test-scoped ("worker fixture accounts
  108 | // cannot depend on a test fixture baseURL") — so it cannot be read from the
  109 | // config and has to be resolved from the same source the config resolves it
  110 | // from. These two must stay in sync or an accounts context would resolve
  111 | // relative URLs differently from the default `page`, which would be a
  112 | // genuinely baffling failure.
  113 | //
  114 | // Same duplication trade-off already accepted for this exact literal in
  115 | // webhooks.js's generation-payload route (see DECISIONS.md) — there is no
  116 | // shared config module to import from, and one string does not justify one.
  117 | const DEFAULT_BASE_URL = process.env.TARGET_URL || 'https://service-desk-roan.vercel.app'
  118 | 
  119 | /**
  120 |  * Builds a `test` with an `accounts` fixture bound to one project's login
  121 |  * flow. Each project wires this up once in helpers/project-<id>/fixtures.ts;
  122 |  * generated specs import that, never this file directly.
  123 |  */
  124 | export function makeAccountsTest(authenticate: Authenticate, options: AccountsFixtureOptions = {}) {
  125 |   return base.extend<Record<string, never>, { accounts: Accounts }>({
  126 |     accounts: [
  127 |       async ({ browser }, use) => {
  128 |         const contexts = new Map<string, BrowserContext>()
  129 | 
  130 |         const accounts: Accounts = {
  131 |           credentialsFor: credentialsFromEnv,
  132 | 
  133 |           async contextAs(alias: string): Promise<BrowserContext> {
  134 |             const cached = contexts.get(alias)
  135 |             if (cached) return cached
  136 | 
  137 |             const credentials = credentialsFromEnv(alias)
  138 |             const context = await browser.newContext({
  139 |               // Manually-created contexts get none of the project's `use`
  140 |               // block, so baseURL has to be passed explicitly or every
  141 |               // relative page.goto('/…') in the test fails. A project can
  142 |               // override it via contextOptions.
  143 |               baseURL: DEFAULT_BASE_URL,
  144 |               ...(options.contextOptions ?? {}),
  145 |             })
  146 | 
  147 |             const page = await context.newPage()
  148 |             try {
  149 |               await authenticate(page, credentials)
  150 |             } catch (error) {
  151 |               await context.close()
  152 |               const reason = error instanceof Error ? error.message : String(error)
  153 |               // Distinguishes "this account cannot sign in" from "the feature
  154 |               // under test is broken" — without this the failure reads as a
  155 |               // product defect in the client's report, which is worse than
  156 |               // useless.
> 157 |               throw new Error(`Could not authenticate test account "${alias}": ${reason}`)
      |                     ^ Error: Could not authenticate test account "client_single_project": locator.fill: Timeout 30000ms exceeded.
  158 |             }
  159 |             // Keep the context (it holds the session); drop the login page so
  160 |             // pageAs() always hands back a clean one.
  161 |             await page.close()
  162 | 
  163 |             contexts.set(alias, context)
  164 |             return context
  165 |           },
  166 | 
  167 |           async pageAs(alias: string): Promise<Page> {
  168 |             const context = await this.contextAs(alias)
  169 |             return context.newPage()
  170 |           },
  171 |         }
  172 | 
  173 |         await use(accounts)
  174 | 
  175 |         // Worker teardown. Contexts are cached in memory for the life of the
  176 |         // worker and never written to .auth/ — two workers resolving the same
  177 |         // alias would race on the same file, and a session reused across runs
  178 |         // expires into an intermittent failure that looks like flake.
  179 |         for (const context of contexts.values()) {
  180 |           await context.close()
  181 |         }
  182 |       },
  183 |       { scope: 'worker' },
  184 |     ],
  185 |   })
  186 | }
  187 | 
  188 | export { expect }
  189 | 
```