# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: generated/p9-e2e-critical-flows/tc-31-client-signs-in-and-lands-on-their-project-dashboard-showing.spec.ts >> TC-31 — Client signs in and lands on their project dashboard showing quality intelligence >> TC-31: Client signs in and lands on their project dashboard showing quality intelligence
- Location: tests/generated/p9-e2e-critical-flows/tc-31-client-signs-in-and-lands-on-their-project-dashboard-showing.spec.ts:7:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/Launch Ready|Not Ready/)
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/Launch Ready|Not Ready/)

```

```yaml
- complementary:
  - text: Blueprint
  - button "Sauce Labs Demo App"
  - text: Project
  - link "Dashboard":
    - /url: /projects/4
    - img
    - text: Dashboard
  - link "Views":
    - /url: /projects/4/views
    - img
    - text: Views
  - button "Change profile picture": A
  - text: Acme Client
  - button "Log out":
    - img
- main:
  - button "Search test cases and bugs":
    - img
    - text: Search /
  - text: Sauce Labs Demo App
  - button "Recent activity":
    - img
  - img
  - text: No links added yet. Good evening, Acme
  - heading "Sauce Labs Demo App is just getting started." [level=1]
  - text: Run your first execution to see a quality score here. Quality score —
  - link "Requirements":
    - /url: /projects/4/requirements
    - img
    - text: Requirements
  - link "Test cases":
    - /url: /projects/4/tests
    - img
    - text: Test cases
  - link "Executions":
    - /url: /projects/4/executions
    - img
    - text: Executions
  - link "Bug reports":
    - /url: /projects/4/bugs
    - img
    - text: Bug reports
  - link "Automation":
    - /url: /projects/4/automation
    - img
    - text: Automation
  - text: Tests 0 No test cases yet Open bugs 2 1 high priority Automated — No test cases yet Req. coverage 0% 0 of 3 covered Feature health
  - link "Bugs":
    - /url: /projects/4/bugs
    - text: Bugs
    - img
  - text: 01 — Bug tracking 1 high 1 medium No data 02 — Authentication Not tracked yet No data 03 — Automation Not tracked yet No data Value delivered Caught by automation 0 issues found this month before reaching your customers Automated coverage — No test cases yet Pass rate trend — No executions this week yet Awaiting your approval
  - link "2 requirements to review":
    - /url: /projects/4/approvals?type=requirements&approval=pending
    - strong: "2"
    - text: requirements to review
    - img
  - text: Pass rate — recent runs
  - link "Executions":
    - /url: /projects/4/executions
    - text: Executions
    - img
  - text: Run your first execution to start tracking trends over time. Uncovered requirements
  - link "Requirements":
    - /url: /projects/4/requirements
    - text: Requirements
    - img
  - text: 3 requirements with no test coverage yet Recent automation runs
  - link "Automation":
    - /url: /projects/4/automation
    - text: Automation
    - img
  - text: Regression Pack 100% (5/5) 2 hours ago New bugs reported (3)
  - link "Bugs":
    - /url: /projects/4/bugs
    - text: Bugs
    - img
  - text: Report download link expires too early 2 hours ago Bug list filter resets when returning from detail view 2 hours ago Dashboard quality score renders stale after platform switch 2 hours ago
- button "Tour":
  - img
  - text: Tour
- text: 👋 Quick tour
- button "Skip tour"
- text: Welcome to Blueprint Here's a 60-second look at what you're seeing and how to read it.
- button "Start"
```

# Test source

```ts
  1  | // spec: specs/tc-31-client-signs-in-and-lands-on-their-project-dashboard-showing.md
  2  | // seed: tests/seed.spec.ts
  3  | 
  4  | import { test, expect } from '../../../helpers/project-9/fixtures';
  5  | 
  6  | test.describe('TC-31 — Client signs in and lands on their project dashboard showing quality intelligence', () => {
  7  |   test('TC-31: Client signs in and lands on their project dashboard showing quality intelligence', async ({ accounts }) => {
  8  |     // Several full page navigations (each a fresh SPA load), give this more headroom than the
  9  |     // default test timeout (timing only).
  10 |     test.setTimeout(60_000);
  11 | 
  12 |     let projectId = '';
  13 | 
  14 |     // 1. Navigate to the BluePrint sign-in page
  15 |     // 2. Enter valid credentials for the `client_single_project` test account and submit
  16 |     // Both handled by the accounts fixture's authenticate() (helpers/project-9/fixtures.ts) — no
  17 |     // raw login steps in the test body, per this project's multi-identity convention.
  18 |     const client = await accounts.pageAs('client_single_project');
  19 | 
  20 |     // 3. Verify the browser is redirected directly to the client's own project view (no staff
  21 |     // project list, and no project switcher, shown)
  22 |     await test.step('Verify the browser is redirected directly to the client\'s own project view (no staff project list, and no project switcher, shown)', async () => {
  23 |       await client.goto('/');
  24 | 
  25 |       // The home page ("/") itself redirects a signed-in client straight to their own numbered
  26 |       // project — never a bare "/projects" staff listing.
  27 |       await expect(client).toHaveURL(/\/projects\/\d+$/);
  28 |       const match = client.url().match(/\/projects\/(\d+)/);
  29 |       projectId = match?.[1] ?? '';
  30 |       expect(projectId).not.toBe('');
  31 | 
  32 |       // Confirmed live: this client's sidebar ("complementary" landmark) exposes exactly two
  33 |       // links — Dashboard and Views for their one project — no list of multiple projects and no
  34 |       // project-switcher control (clicking the project-name button in the sidebar produced no
  35 |       // dropdown/menu of other projects).
  36 |       const sidebarLinks = client.getByRole('complementary').getByRole('link');
  37 |       await expect(sidebarLinks).toHaveCount(2);
  38 |       await expect(client.getByRole('link', { name: 'Dashboard' })).toBeVisible();
  39 |       await expect(client.getByRole('link', { name: 'Views' })).toBeVisible();
  40 |     });
  41 | 
  42 |     // 4. Confirm the dashboard displays the current Quality Score, health status, and launch
  43 |     // readiness recommendation
  44 |     await test.step('Confirm the dashboard displays the current Quality Score, health status, and launch readiness recommendation', async () => {
  45 |       await expect(client.getByText('Quality score', { exact: true })).toBeVisible();
  46 |       // "Feature health" is this dashboard's health-status panel (ranked per-feature health).
  47 |       await expect(client.getByText('Feature health', { exact: true })).toBeVisible();
  48 | 
  49 |       // The launch-readiness recommendation renders as a literal "Launch Ready"/"Not Ready" badge
  50 |       // (id="tour-launch-readiness", introduced in the app's own onboarding tour as "Status &
  51 |       // launch readiness") once a project has enough quality-score data — confirmed directly from
  52 |       // the live app's shipped JS bundle. The only session available during generation
  53 |       // (structurally a single-project client, same as client_single_project, but with zero
  54 |       // executions recorded on its one project) never mounts this widget at all in that
  55 |       // zero-data state, so this exact text could not be observed actually rendering live.
  56 |       // client_single_project is expected to carry real quality data for this scenario; flag to
  57 |       // the reviewer if this doesn't hold.
> 58 |       await expect(client.getByText(/Launch Ready|Not Ready/)).toBeVisible();
     |                                                                ^ Error: expect(locator).toBeVisible() failed
  59 |     });
  60 | 
  61 |     // 5. Confirm the dashboard shows no link, section, or reference to Generation History, the
  62 |     // Engineering dashboard, AI healing, or agent-run details
  63 |     await test.step('Confirm the dashboard shows no link, section, or reference to Generation History, the Engineering dashboard, AI healing, or agent-run details', async () => {
  64 |       await expect(client.getByText(/Generation History|Engineering dashboard|AI healing|agent-run/i)).toHaveCount(0);
  65 |       await expect(client.getByRole('link', { name: /Generation History|Engineering|Healing/i })).toHaveCount(0);
  66 |     });
  67 | 
  68 |     // 6. Attempt to navigate to the Generation History URL directly
  69 |     await test.step('Attempt to navigate to the Generation History URL directly', async () => {
  70 |       await client.goto(`/projects/${projectId}/automation/history`);
  71 |     });
  72 | 
  73 |     // 7. Verify the browser redirects back to the client's project rather than showing the page
  74 |     // or an error
  75 |     await test.step("Verify the browser redirects back to the client's project rather than showing the page or an error", async () => {
  76 |       // Confirmed live: this staff-only route silently redirects a client back to the
  77 |       // project's own (client-accessible) Automation overview instead of the Generation
  78 |       // History page — never the requested page, never an error.
  79 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/automation$`));
  80 |       await expect(client.getByText('Page not found')).toHaveCount(0);
  81 |     });
  82 | 
  83 |     // 8. Attempt to navigate to the Engineering dashboard URL directly
  84 |     await test.step('Attempt to navigate to the Engineering dashboard URL directly', async () => {
  85 |       await client.goto(`/projects/${projectId}/engineering`);
  86 |     });
  87 | 
  88 |     // 9. Verify the browser redirects back to the client's project rather than showing the page
  89 |     // or an error
  90 |     await test.step("Verify the browser redirects back to the client's project rather than showing the page or an error", async () => {
  91 |       // Confirmed live: same silent redirect behaviour as the Generation History route above —
  92 |       // lands back on the project's own Automation overview, never the Engineering page or an
  93 |       // error.
  94 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/automation$`));
  95 |       await expect(client.getByText('Page not found')).toHaveCount(0);
  96 |     });
  97 |   });
  98 | });
  99 | 
```