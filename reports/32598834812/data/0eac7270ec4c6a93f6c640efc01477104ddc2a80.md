# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: generated/p9-e2e-critical-flows/p9-tc-40-runs-and-bugs-created-during-a-paused-window-remain-hidden-f.spec.ts >> TC-40 — Runs and bugs created during a paused window remain hidden from the client after resume >> TC-40: Runs and bugs created during a paused window remain hidden from the client after resume
- Location: tests/generated/p9-e2e-critical-flows/p9-tc-40-runs-and-bugs-created-during-a-paused-window-remain-hidden-f.spec.ts:66:3

# Error details

```
TimeoutError: locator.click: Timeout 30000ms exceeded.
Call log:
  - waiting for getByRole('button', { name: '+ Log bug' })

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - complementary [ref=e4]:
    - generic [ref=e7]: Blueprint
    - button "Sauce Labs Demo App" [ref=e9]:
      - generic [ref=e10]: Sauce Labs Demo App
    - generic [ref=e11]:
      - generic [ref=e12]: Project
      - link "Dashboard" [ref=e13] [cursor=pointer]:
        - /url: /projects/4
        - img [ref=e14]
        - text: Dashboard
      - link "Views" [ref=e16] [cursor=pointer]:
        - /url: /projects/4/views
        - img [ref=e17]
        - text: Views
      - link "Visual" [ref=e20] [cursor=pointer]:
        - /url: /projects/4/visual
        - img [ref=e21]
        - text: Visual
    - generic [ref=e24]:
      - button "Change profile picture" [ref=e25] [cursor=pointer]:
        - generic [ref=e26]: A
      - generic [ref=e27]:
        - generic "Acme 2 — click to edit" [ref=e28] [cursor=pointer]: Acme
        - generic "Client — click to edit" [ref=e29] [cursor=pointer]: Client
      - button "Log out" [ref=e30] [cursor=pointer]:
        - img [ref=e31]
  - main [ref=e34]:
    - button "Search test cases and bugs" [ref=e35] [cursor=pointer]:
      - img [ref=e36]
      - generic [ref=e39]: Search
      - generic [ref=e40]: /
    - generic [ref=e42]:
      - button "Back" [ref=e43] [cursor=pointer]:
        - img [ref=e44]
      - generic [ref=e46]:
        - link "Sauce Labs Demo App" [ref=e47] [cursor=pointer]:
          - /url: /projects/4
        - generic [ref=e48]: /
        - generic [ref=e49]: Approvals
    - generic [ref=e50]:
      - generic [ref=e51]:
        - button "Requirements" [ref=e52] [cursor=pointer]
        - button "Test cases" [ref=e53] [cursor=pointer]
      - generic [ref=e54]:
        - generic [ref=e55]:
          - generic [ref=e56]: Platform
          - button "All" [ref=e57] [cursor=pointer]
          - button "Web" [ref=e58] [cursor=pointer]
          - button "iOS" [ref=e59] [cursor=pointer]
          - button "Android" [ref=e60] [cursor=pointer]
        - generic [ref=e61]:
          - generic [ref=e62]: Approval
          - button "All" [ref=e63] [cursor=pointer]
          - button "Pending" [ref=e64] [cursor=pointer]
          - button "Changes requested" [ref=e65] [cursor=pointer]
          - button "Ready for review" [ref=e66] [cursor=pointer]
          - button "Approved" [ref=e67] [cursor=pointer]
      - button "Approve all" [ref=e69] [cursor=pointer]
      - table [ref=e72]:
        - rowgroup [ref=e73]:
          - row "Select all approvable rows Title Feature Platform Approval" [ref=e74]:
            - columnheader "Select all approvable rows" [ref=e75]:
              - checkbox "Select all approvable rows" [ref=e76]
            - columnheader "Title" [ref=e77]
            - columnheader "Feature" [ref=e78]
            - columnheader "Platform" [ref=e79]
            - columnheader "Approval" [ref=e80]
        - rowgroup [ref=e81]:
          - row "Archived bugs drop out of the severity breakdown Authentication Web Approved" [ref=e82] [cursor=pointer]:
            - cell [ref=e83]
            - cell "Archived bugs drop out of the severity breakdown" [ref=e84]:
              - generic [ref=e85]: Archived bugs drop out of the severity breakdown
            - cell "Authentication" [ref=e86]
            - cell "Web" [ref=e87]:
              - generic [ref=e88]: Web
            - cell "Approved" [ref=e89]
          - row "Bug comments are attributed to their author Authentication Web Approved" [ref=e90] [cursor=pointer]:
            - cell [ref=e91]
            - cell "Bug comments are attributed to their author" [ref=e92]:
              - generic [ref=e93]: Bug comments are attributed to their author
            - cell "Authentication" [ref=e94]
            - cell "Web" [ref=e95]:
              - generic [ref=e96]: Web
            - cell "Approved" [ref=e97]
          - row "Execution runs can be excluded from the quality score Authentication Web Approved" [ref=e98] [cursor=pointer]:
            - cell [ref=e99]
            - cell "Execution runs can be excluded from the quality score" [ref=e100]:
              - generic [ref=e101]: Execution runs can be excluded from the quality score
            - cell "Authentication" [ref=e102]
            - cell "Web" [ref=e103]:
              - generic [ref=e104]: Web
            - cell "Approved" [ref=e105]
          - row "Automation reports are downloadable for completed runs Authentication Web Approved" [ref=e106] [cursor=pointer]:
            - cell [ref=e107]
            - cell "Automation reports are downloadable for completed runs" [ref=e108]:
              - generic [ref=e109]: Automation reports are downloadable for completed runs
            - cell "Authentication" [ref=e110]
            - cell "Web" [ref=e111]:
              - generic [ref=e112]: Web
            - cell "Approved" [ref=e113]
          - row "Clients can comment on bugs without edit rights Authentication Web Pending" [ref=e114] [cursor=pointer]:
            - cell [ref=e115]:
              - checkbox [ref=e116]
            - cell "Clients can comment on bugs without edit rights" [ref=e117]:
              - generic [ref=e118]: Clients can comment on bugs without edit rights
            - cell "Authentication" [ref=e119]
            - cell "Web" [ref=e120]:
              - generic [ref=e121]: Web
            - cell "Pending" [ref=e122]
          - row "Client dashboard shows a per-platform quality score Authentication Web Pending" [ref=e123] [cursor=pointer]:
            - cell [ref=e124]:
              - checkbox [ref=e125]
            - cell "Client dashboard shows a per-platform quality score" [ref=e126]:
              - generic [ref=e127]: Client dashboard shows a per-platform quality score
            - cell "Authentication" [ref=e128]
            - cell "Web" [ref=e129]:
              - generic [ref=e130]: Web
            - cell "Pending" [ref=e131]
```

# Test source

```ts
  62  |       console.warn('TC-40 cleanup: failed to resume maintenance mode', e);
  63  |     }
  64  |   });
  65  | 
  66  |   test('TC-40: Runs and bugs created during a paused window remain hidden from the client after resume', async ({ accounts }) => {
  67  |     // Many steps across two actors and multiple page navigations — give extra headroom.
  68  |     test.setTimeout(120_000);
  69  | 
  70  |     let staff: Awaited<ReturnType<typeof accounts.pageAs>>;
  71  |     let client: Awaited<ReturnType<typeof accounts.pageAs>>;
  72  | 
  73  |     // 1. Open a page context as the `staff` alias via `accounts.pageAs('staff')`
  74  |     await test.step("Open a page context as the `staff` alias via `accounts.pageAs('staff')`", async () => {
  75  |       staff = await accounts.pageAs('staff');
  76  |     });
  77  | 
  78  |     // 2. Open a page context as the `client_single_project` alias via `accounts.pageAs('client_single_project')`
  79  |     await test.step("Open a page context as the `client_single_project` alias via `accounts.pageAs('client_single_project')`", async () => {
  80  |       client = await accounts.pageAs('client_single_project');
  81  |     });
  82  | 
  83  |     // 3. As Client, navigate to '/' and verify the URL resolves to /projects/<id>; capture the numeric project ID
  84  |     await test.step("As Client, navigate to '/' and verify the URL resolves to /projects/<id>; capture the numeric project ID", async () => {
  85  |       await client.goto('/');
  86  |       await expect(client).toHaveURL(/\/projects\/\d+$/);
  87  |       const match = client.url().match(/\/projects\/(\d+)/);
  88  |       projectId = match?.[1] ?? '';
  89  |       expect(projectId).not.toBe('');
  90  |     });
  91  | 
  92  |     // 4. As Staff, navigate to Project A's automation page (/projects/<id>/automation)
  93  |     await test.step("As Staff, navigate to Project A's automation page (/projects/<id>/automation)", async () => {
  94  |       await staff.goto(`/projects/${projectId}/automation`);
  95  |       await expect(staff).toHaveURL(new RegExp(`/projects/${projectId}/automation$`));
  96  |     });
  97  | 
  98  |     // Pre-step 5: Normalize state — if a prior failed run left maintenance mode ON,
  99  |     // click Resume now so we always enter step 5 from a known clean state.
  100 |     await test.step('Normalize: ensure maintenance mode is OFF before step 5', async () => {
  101 |       const maintenanceBtn = staff.getByRole('button', { name: 'Maintenance mode' });
  102 |       const resumeBtn = staff.getByRole('button', { name: 'Resume' }).first();
  103 |       // Wait for the automation page to settle: one of the two mutually-exclusive
  104 |       // buttons must appear before we can decide which state we are in.
  105 |       await Promise.race([
  106 |         maintenanceBtn.waitFor({ state: 'visible', timeout: 15_000 }),
  107 |         resumeBtn.waitFor({ state: 'visible', timeout: 15_000 }),
  108 |       ]);
  109 |       const isAlreadyPaused = await resumeBtn.isVisible().catch(() => false);
  110 |       if (isAlreadyPaused) {
  111 |         await resumeBtn.click();
  112 |         await expect(maintenanceBtn).toBeVisible({ timeout: 15_000 });
  113 |       }
  114 |     });
  115 | 
  116 |     // 5. As Staff, click the Maintenance Mode button to pause Project A
  117 |     await test.step('As Staff, click the Maintenance Mode button to pause Project A', async () => {
  118 |       await staff.getByRole('button', { name: 'Maintenance mode' }).click();
  119 |     });
  120 | 
  121 |     // 6. Verify the automation page shows Project A is now in a paused/maintenance state
  122 |     await test.step('Verify the automation page shows Project A is now in a paused/maintenance state', async () => {
  123 |       // After clicking, the Maintenance mode button is replaced by an active Resume button.
  124 |       // Both a banner and the button confirm the paused state.
  125 |       await expect(staff.getByRole('button', { name: 'Resume' }).first()).toBeVisible();
  126 |     });
  127 | 
  128 |     // 7. As Staff, navigate to Project A's executions page (/projects/<id>/executions)
  129 |     await test.step("As Staff, navigate to Project A's executions page (/projects/<id>/executions)", async () => {
  130 |       await staff.goto(`/projects/${projectId}/executions`);
  131 |       await expect(staff).toHaveURL(new RegExp(`/projects/${projectId}/executions$`));
  132 |     });
  133 | 
  134 |     // 8. As Staff, create a new execution run with a unique title
  135 |     await test.step('As Staff, create a new execution run with a unique title', async () => {
  136 |       runTitle = `TC-40 run ${createTestData().title}`;
  137 |       await staff.getByRole('button', { name: '+ New execution run' }).click();
  138 |       await staff.getByPlaceholder('e.g. Sprint 14 regression').fill(runTitle);
  139 |       // Single combobox on executions page when the modal is open (platform selector).
  140 |       await staff.getByRole('combobox').selectOption({ label: 'Web' });
  141 |       // FRAGILE: .checkbox-list scopes to the test-case list so other modal
  142 |       // checkboxes (exclude-from-score, etc.) are not accidentally selected.
  143 |       // Established pattern from TC-33 step 8.
  144 |       await staff.locator('.checkbox-list').getByRole('checkbox').first().check();
  145 |       await staff.getByRole('button', { name: 'Create run' }).click();
  146 |     });
  147 | 
  148 |     // 9. Verify the newly created execution run appears in the executions list
  149 |     await test.step('Verify the newly created execution run appears in the executions list', async () => {
  150 |       await expect(staff.getByRole('link', { name: runTitle })).toBeVisible();
  151 |     });
  152 | 
  153 |     // 10. As Staff, navigate to Project A's bugs page (/projects/<id>/bugs)
  154 |     await test.step("As Staff, navigate to Project A's bugs page (/projects/<id>/bugs)", async () => {
  155 |       await staff.goto(`/projects/${projectId}/bugs`);
  156 |       await expect(staff).toHaveURL(new RegExp(`/projects/${projectId}/bugs$`));
  157 |     });
  158 | 
  159 |     // 11. As Staff, create a new bug with a unique title
  160 |     await test.step('As Staff, create a new bug with a unique title', async () => {
  161 |       bugTitle = `TC-40 bug ${createTestData().title}`;
> 162 |       await staff.getByRole('button', { name: '+ Log bug' }).click();
      |                                                              ^ TimeoutError: locator.click: Timeout 30000ms exceeded.
  163 |       await staff.getByPlaceholder('Short description of the bug').fill(bugTitle);
  164 |       // FRAGILE: the bug form's comboboxes lack explicit label associations.
  165 |       // Within .modal: nth(0)=Execution run, nth(1)=Feature, nth(2)=Platform, nth(3)=Severity.
  166 |       await staff.locator('.modal').getByRole('combobox').nth(1).selectOption({ label: 'Authentication' });
  167 |       await staff.locator('.modal').getByRole('combobox').nth(2).selectOption({ label: 'Web' });
  168 |       await staff.getByRole('button', { name: 'Log bug', exact: true }).click();
  169 |     });
  170 | 
  171 |     // 12. Verify the newly created bug appears in the bugs list
  172 |     await test.step('Verify the newly created bug appears in the bugs list', async () => {
  173 |       // FRAGILE: bug rows carry no ARIA role — .bug-row is the hand-authored class confirmed live.
  174 |       await expect(staff.locator('.bug-row').filter({ hasText: bugTitle })).toBeVisible();
  175 |     });
  176 | 
  177 |     // 13. As Client, navigate to Project A's executions page (/projects/<id>/executions)
  178 |     await test.step("As Client, navigate to Project A's executions page (/projects/<id>/executions)", async () => {
  179 |       await client.goto(`/projects/${projectId}/executions`);
  180 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/executions$`));
  181 |     });
  182 | 
  183 |     // 14. Verify the execution run created in step 8 DOES appear in the client's runs list.
  184 |     // Only automation-suite runs are filtered during maintenance mode; manually created
  185 |     // execution runs remain visible to the client — this is the intended behavior.
  186 |     await test.step("Verify the execution run created in step 8 appears in the client's runs list", async () => {
  187 |       // FRAGILE: .card is the hand-authored class for executions-page run rows.
  188 |       await expect(client.getByRole('link', { name: runTitle })).toBeVisible({ timeout: 30_000 });
  189 |     });
  190 | 
  191 |     // 15. As Client, navigate to Project A's bugs page (/projects/<id>/bugs)
  192 |     await test.step("As Client, navigate to Project A's bugs page (/projects/<id>/bugs)", async () => {
  193 |       await client.goto(`/projects/${projectId}/bugs`);
  194 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/bugs$`));
  195 |     });
  196 | 
  197 |     // 16. Verify the bug created in step 11 DOES appear in the client's bugs list.
  198 |     // Only automation-suite runs are filtered during maintenance mode; manually created
  199 |     // bugs remain visible to the client — this is the intended behavior.
  200 |     await test.step("Verify the bug created in step 11 appears in the client's bugs list", async () => {
  201 |       // FRAGILE: .bug-row is the hand-authored class confirmed live.
  202 |       await expect(client.locator('.bug-row').filter({ hasText: bugTitle })).toBeVisible({ timeout: 30_000 });
  203 |     });
  204 | 
  205 |     // 17. As Staff, navigate to Project A's automation page (/projects/<id>/automation)
  206 |     await test.step("As Staff, navigate to Project A's automation page (/projects/<id>/automation)", async () => {
  207 |       await staff.goto(`/projects/${projectId}/automation`);
  208 |       await expect(staff).toHaveURL(new RegExp(`/projects/${projectId}/automation$`));
  209 |     });
  210 | 
  211 |     // 18. As Staff, click the Maintenance Mode button to resume Project A
  212 |     await test.step('As Staff, click the Maintenance Mode button to resume Project A', async () => {
  213 |       // Two Resume buttons exist in the paused state (banner + Suites section);
  214 |       // clicking the first is sufficient.
  215 |       await staff.getByRole('button', { name: 'Resume' }).first().click();
  216 |     });
  217 | 
  218 |     // 19. Verify the automation page no longer shows a paused/maintenance-mode indicator
  219 |     await test.step('Verify the automation page no longer shows a paused/maintenance-mode indicator', async () => {
  220 |       await expect(staff.getByRole('button', { name: 'Maintenance mode' })).toBeVisible();
  221 |       await expect(staff.getByRole('button', { name: 'Resume' })).toHaveCount(0);
  222 |     });
  223 | 
  224 |     // 20. As Client, navigate to Project A's executions page (/projects/<id>/executions)
  225 |     await test.step("As Client, navigate to Project A's executions page (/projects/<id>/executions) after resume", async () => {
  226 |       await client.goto(`/projects/${projectId}/executions`);
  227 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/executions$`));
  228 |     });
  229 | 
  230 |     // 21. Verify the execution run created in step 8 IS visible in the client's runs list after resume.
  231 |     // Manually created runs are never filtered — they remain visible before and after resume.
  232 |     await test.step("Verify the execution run created in step 8 is visible in the client's runs list after resume", async () => {
  233 |       // FRAGILE: .card is the hand-authored class for executions-page run rows.
  234 |       await expect(client.getByRole('link', { name: runTitle })).toBeVisible({ timeout: 30_000 });
  235 |     });
  236 | 
  237 |     // 22. As Client, navigate to Project A's bugs page (/projects/<id>/bugs)
  238 |     await test.step("As Client, navigate to Project A's bugs page (/projects/<id>/bugs)", async () => {
  239 |       await client.goto(`/projects/${projectId}/bugs`);
  240 |       await expect(client).toHaveURL(new RegExp(`/projects/${projectId}/bugs$`));
  241 |     });
  242 | 
  243 |     // 23. Verify the bug created in step 11 IS visible in the client's bugs list after resume.
  244 |     // Manually created bugs are never filtered — they remain visible before and after resume.
  245 |     await test.step("Verify the bug created in step 11 is visible in the client's bugs list after resume", async () => {
  246 |       // FRAGILE: .bug-row is the hand-authored class confirmed live.
  247 |       await expect(client.locator('.bug-row').filter({ hasText: bugTitle })).toBeVisible({ timeout: 30_000 });
  248 |     });
  249 |   });
  250 | });
  251 | 
```